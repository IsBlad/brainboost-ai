// Alert when click Save
const edit = document.querySelectorAll(".edit-button");

edit.forEach(edit => {
    edit.addEventListener("click", function() {
        alert("Sorry this is a future feature!");
    });
});